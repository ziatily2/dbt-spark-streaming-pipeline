snapshots:
  - name: FactTrips
    relation: ref('trips')
    config:
      schema: gold
      database: pysparkdbt
      unique_key: trip_id
      strategy: timestamp
      updated_at: last_updated_timestamp
      dbt_valid_to_current: "to_date('9999-12-31')" 